name: walstream-e2e

services:
  minio:
    image: quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z
    command:
      - server
      - /data
      - --address
      - :9000
    environment:
      MINIO_ROOT_USER: walstream
      MINIO_ROOT_PASSWORD: walstream-secret
